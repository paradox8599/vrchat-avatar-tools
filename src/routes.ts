export enum ROUTES {
  start = "/",
  update = "/update",
  login = "/login",
  settings = "/settings",
  publicCheck = "/public-check",
  userAvatars = "/user-avatars",
}

export const ROUTE_HOME = ROUTES.userAvatars;
