export enum ROUTES {
  start = "/",
  update = "/update",
  login = "/login",
  settings = "/settings",
  publicCheck = "/public-check",
  avatars = "/avatars",
}

export const ROUTE_HOME = ROUTES.publicCheck;
